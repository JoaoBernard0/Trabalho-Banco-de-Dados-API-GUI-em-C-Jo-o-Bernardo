<Window x:Class="InventoryGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inventory GUI" Height="600" Width="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBox x:Name="SearchBox" Width="250" />
            <Button Content="Buscar" Click="OnSearch" Margin="8,0,0,0"/>
            <Button Content="Novo Produto" Click="OnNewProduct" Margin="16,0,0,0"/>
        </StackPanel>

        <DataGrid x:Name="ProductsGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Preço" Binding="{Binding Price}" Width="120"/>
                <DataGridTextColumn Header="Categoria" Binding="{Binding CategoryName}" Width="200"/>
                <DataGridTextColumn Header="Criado" Binding="{Binding CreatedAt}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Editar" Click="OnEdit" Margin="0,0,8,0"/>
            <Button Content="Deletar" Click="OnDelete"/>
        </StackPanel>
    </Grid>
</Window>
